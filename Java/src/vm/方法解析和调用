方法(Method)解析和调用分静态和动态2种

解析(Resolution)	
	编译期可知，运行期不可变：包括静态方法和私有方法。在类加载阶段, 符号引用就被直接转化为直接引用

方法重载(Overload)
	静态解析, 多个版本的重载函数，调用函数时检查参数的引用类型(不检查其具体类型)，也是编译期可知的
	
方法重写(Override)
	动态解析, 检查调用方法的对象的具体类型, 运行期才可知
	类的方法区中有一个虚方法表(vtable), 接口执行时也有一个接口方法表(itable), 记录此类中所有方法的具体入口地址
		方法表在类加载的初始化阶段被初始化完毕